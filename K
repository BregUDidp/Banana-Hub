local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
   Name = "   |  Akari Hub   [ V-3.0 ]",
   Icon = "webhook",
   LoadingTitle = "[ 🕸️ ]  Akari Hub  V. Zero   [ 🕸️ ]",
   LoadingSubtitle = "____________________________________________________________________________________________________________________________________________________________________________________________________________________________________________________________________________________________________________",
   ShowText = "🕸️",
   Theme = "ThemeIdentifier",
   ToggleUIKeybind = "G",
   DisableRayfieldPrompts = true,
   DisableBuildWarnings = true,
   ConfigurationSaving = {
      Enabled = true,
      FolderName = nil,
      FileName = "Big Hub"
   },
   Discord = {
      Enabled = false,
      Invite = "noinvitelink",
      RememberJoins = true
   },
   KeySystem = false,
   KeySettings = {
      Title = "",
      Subtitle = "",
      Note = "",
      FileName = "",
      SaveKey = true,
      GrabKeyFromSite = false,
      Key = {"🕸️"}
   }
})

--// Services
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local Workspace = game:GetService("Workspace")
local LocalPlayer = Players.LocalPlayer

--// === PLAYER TAB ===
local PTab = Window:CreateTab("||   - Player", "bot")
local Section = PTab:CreateSection("")
 
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")

local player = Players.LocalPlayer
local camera = workspace.CurrentCamera
local character = player.Character or player.CharacterAdded:Wait()

UserInputService.InputEnded:Connect(function(input)
	if input.KeyCode == Enum.KeyCode.Space then
		ascend = false
	elseif input.KeyCode == Enum.KeyCode.LeftShift then
		descend = false
	end
end)

-- ตัวแปรระบบ
local Players = game:GetService("Players")
local RS = game:GetService("RunService")
local UIS = game:GetService("UserInputService")
local player = Players.LocalPlayer
local char = player.Character or player.CharacterAdded:Wait()
local hrp = char:WaitForChild("HumanoidRootPart")
local humanoid = char:WaitForChild("Humanoid")
 
-- ตัวแปรฟังก์ชัน
local flying = false
local vv, gyro
local flySpeed = 50
local superSpeed = false
local runSpeed = 16
local noclipEnabled = false
local infJumpEnabled = false
 
-- ฟังก์ชันวิ่งเร็ว
local function toggleSpeed(on)
    superSpeed = on
    humanoid.WalkSpeed = superSpeed and runSpeed or 16
    Rayfield:Notify({
        Title = superSpeed and "🏃‍♂️ Speed Run ✅" or "🚶‍♂️ Speed Run 🛑",
        Content = superSpeed and "Become Speed Flash!" or "Become Speed Normal",
        Duration = 3,
        Image = "bike",
    })
end

-- Services
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local RunService = game:GetService("RunService")

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")

local player = Players.LocalPlayer
local flying = false
local flySpeed = 50
local flyConnection
local bodyVelocity
local bodyGyro

local function updateFly()
    local character = player.Character
    if not character or not flying then return end
    
    local humanoidRootPart = character:FindFirstChild("HumanoidRootPart")
    if not humanoidRootPart then return end
    
    local camera = workspace.CurrentCamera
    local horizontal = Vector3.new(0, 0, 0)
    
    if UserInputService:IsKeyDown(Enum.KeyCode.W) then
        horizontal = horizontal + camera.CFrame.LookVector
    end
    if UserInputService:IsKeyDown(Enum.KeyCode.S) then
        horizontal = horizontal - camera.CFrame.LookVector
    end
    if UserInputService:IsKeyDown(Enum.KeyCode.A) then
        horizontal = horizontal - camera.CFrame.RightVector
    end
    if UserInputService:IsKeyDown(Enum.KeyCode.D) then
        horizontal = horizontal + camera.CFrame.RightVector
    end
    
    local vertical = 0
    if UserInputService:IsKeyDown(Enum.KeyCode.Space) then
        vertical = 1
    elseif UserInputService:IsKeyDown(Enum.KeyCode.LeftShift) then
        vertical = -1
    end
    
    local moveDir = horizontal + Vector3.new(0, vertical, 0)
    if moveDir.Magnitude > 0 then
        bodyVelocity.Velocity = moveDir.Unit * flySpeed
    else
        bodyVelocity.Velocity = Vector3.new(0, 0, 0)
    end
    
    bodyGyro.CFrame = camera.CFrame
end

local FlyToggle = PTab:CreateToggle({
    Name = "[ 🛸 ]  Flying Jetpack",
    CurrentValue = false,
    Flag = "FlyEnabled",
    Callback = function(Value)
        flying = Value
        
        local character = player.Character
        if not character then return end
        
        local humanoid = character:FindFirstChild("Humanoid")
        local humanoidRootPart = character:FindFirstChild("HumanoidRootPart")
        if not humanoid or not humanoidRootPart then return end
        
        if Value then
            humanoid.PlatformStand = true
            
            bodyVelocity = Instance.new("BodyVelocity")
            bodyVelocity.MaxForce = Vector3.new(9e9, 9e9, 9e9)
            bodyVelocity.Velocity = Vector3.new(0, 0, 0)
            bodyVelocity.Parent = humanoidRootPart
            
            bodyGyro = Instance.new("BodyGyro")
            bodyGyro.MaxTorque = Vector3.new(9e9, 9e9, 9e9)
            bodyGyro.CFrame = humanoidRootPart.CFrame
            bodyGyro.Parent = humanoidRootPart
            
            flyConnection = RunService.Heartbeat:Connect(updateFly)
        else
            humanoid.PlatformStand = false
            if bodyVelocity then bodyVelocity:Destroy() end
            if bodyGyro then bodyGyro:Destroy() end
            if flyConnection then flyConnection:Disconnect() end
        end
    end,
})

local SpeedSlider = PTab:CreateSlider({
    Name = "[ 📏 ]  Slider Flying Speed",
    Range = {50, 2000},
    Increment = 5,
    Suffix = " Flying Speed",
    CurrentValue = 50,
    Flag = "FlySpeed",
    Callback = function(Value)
        flySpeed = Value
    end,
})

local Section = PTab:CreateSection("")

local speedToggle = PTab:CreateToggle({Name="[ 🏃‍♀️ ]  Walk Or Run", CurrentValue=false, Flag="SpeedToggle", Callback=toggleSpeed})
local speedSlider = PTab:CreateSlider({Name="[ 📏 ]  Slider Walk Or Run", Range={16,2000}, Increment=1, Suffix=" Run Speed", CurrentValue=16, Flag="RunSpeed", Callback=function(val) runSpeed=val if superSpeed then humanoid.WalkSpeed=runSpeed end end})

local Section = PTab:CreateSection("")

local InfiniteJumpEnabled = false

PTab:CreateToggle({
    Name = "[ 🏋️‍♀️ ]  Infinite Jump Power",
    CurrentValue = false,
    Flag = "InfiniteJumpToggle",
    Callback = function(Value)
        InfiniteJumpEnabled = Value
        if Value then
        Rayfield:Notify({
           Title = "🏋️‍♀️ Power Jump ✅",
           Content = "Become Power Rabbit",
           Duration = 3,
           Image = "rabbit",
        })
        end
    end
})

PTab:CreateSlider({
    Name = "[ 📏 ]  Slider Jump Power",
    Range = {50, 2000},
    Increment = 1,
    Suffix = " Jump Power",
    CurrentValue = 50,
    Flag = "JumpPowerSlider",
    Callback = function(Value)
        local player = game.Players.LocalPlayer
        if player.Character and player.Character:FindFirstChildOfClass("Humanoid") then
            player.Character.Humanoid.JumpPower = Value
        end
    end
})

game:GetService("UserInputService").JumpRequest:Connect(function()
    if InfiniteJumpEnabled then
        local player = game.Players.LocalPlayer
        if player.Character and player.Character:FindFirstChildOfClass("Humanoid") then
            player.Character.Humanoid:ChangeState("Jumping")
        end
    end
end)

local Section = PTab:CreateSection("")

local noclipToggle = PTab:CreateToggle({Name="[ 🧱 ]  Noclips Walls", Callback=function(val) noclipEnabled=val end})
local infJumpToggle = PTab:CreateToggle({Name="[ 🦘 ]  Infinite Jump", CurrentValue=false, Flag="InfJumpToggle", Callback=function(val) infJumpEnabled=val end})

local InfiniteZoomToggle = PTab:CreateToggle({
   Name = "[ 🔎 ]  Infinite Zoom",
   CurrentValue = false,
   Flag = "InfiniteZoomToggle",
   Callback = function(Value)
      local player = game.Players.LocalPlayer
      
      if Value then
         player.CameraMaxZoomDistance = 1000
         player.CameraMinZoomDistance = 0.5
         Rayfield:Notify({
            Title = "[ ✅ ]  Infinite Zoom ON",
            Content = "[ ✅ ]  Infinite Zoom Finish",
            Duration = 3,
         })
      else
         player.CameraMaxZoomDistance = 400
         player.CameraMinZoomDistance = 0.5
         Rayfield:Notify({
            Title = "[ 🛑 ]  Infinite Zoom OFF",
            Content = "[ 🛑 ]  Infinite Zoom Become Normal",
            Duration = 3,
         })
      end
   end,
})

local Section = PTab:CreateSection("")

-- อัปเดตลูป
RS.RenderStepped:Connect(function()
    if flying and vv and gyro and hrp and humanoid then
        local cam = workspace.CurrentCamera
        if not cam then return end
 
        local flatForward = Vector3.new(cam.CFrame.LookVector.X,0,cam.CFrame.LookVector.Z)
        local forward = flatForward.Magnitude > 0 and flatForward.Unit or Vector3.zero
        local right = Vector3.new(cam.CFrame.RightVector.X,0,cam.CFrame.RightVector.Z)
        if right.Magnitude > 0 then right = right.Unit end
 
        local moveVec = Vector3.zero
        if UIS:IsKeyDown(Enum.KeyCode.W) then moveVec = moveVec + Vector3.new(0,0,1) end
        if UIS:IsKeyDown(Enum.KeyCode.S) then moveVec = moveVec + Vector3.new(0,0,-1) end
        if UIS:IsKeyDown(Enum.KeyCode.A) then moveVec = moveVec + Vector3.new(-1,0,0) end
        if UIS:IsKeyDown(Enum.KeyCode.D) then moveVec = moveVec + Vector3.new(1,0,0) end
        if UIS:IsKeyDown(Enum.KeyCode.Space) then moveVec = moveVec + Vector3.new(0,1,0) end
        if UIS:IsKeyDown(Enum.KeyCode.LeftControl) then moveVec = moveVec + Vector3.new(0,-1,0) end
 
        local desired = forward*moveVec.Z + right*moveVec.X + Vector3.new(0,moveVec.Y,0)
        if desired.Magnitude>0 then desired = desired.Unit*flySpeed end
        vv.Velocity = desired
 
        if desired.Magnitude>1 then
            local flatDir = Vector3.new(desired.X,0,desired.Z)
            if flatDir.Magnitude>0 then gyro.CFrame = CFrame.new(hrp.Position,hrp.Position+flatDir) end
        end
    end
end)
 
RS.Stepped:Connect(function()
    if noclipEnabled and char then
        for _,part in pairs(char:GetDescendants()) do
            if part:IsA("BasePart") then part.CanCollide=false end
        end
    end
end)
 
UIS.JumpRequest:Connect(function()
    if infJumpEnabled and humanoid then
        humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
    end
end)

UIS.JumpRequest:Connect(function()
    if infJumpEnabled and humanoid then
        humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
    end
end)
 
-- รีเซ็ตตัวละครเมื่อ respawn
player.CharacterAdded:Connect(function(c)
    char=c
    hrp=c:WaitForChild("HumanoidRootPart")
    humanoid=c:WaitForChild("Humanoid")
    stopFlying()
    toggleSpeed(false)
    noclipEnabled=false
    infJumpEnabled=false
    Rayfield:LoadConfiguration()
end)

--// === TELEPORT TAB ===
local friendTab = Window:CreateTab("||   - Teleport", "map-pin")
local Section1 = friendTab:CreateSection("")

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local player = Players.LocalPlayer

-- ตัวแปรเก็บตำแหน่งเดิมก่อนวาป
local savedCFrame = nil

-- ตัวแปรสำหรับ Auto Teleport
local autoTPConnection = nil
local autoTargetName = nil

-- ตัวแปรสำหรับ ESP
local currentESPHighlight = nil
local espTargetPlayer = nil
local espConnection = nil

-- ตัวแปรสี ESP (เริ่มต้นเขียวอ่อน)
local espFillColor = Color3.fromRGB(43, 179, 123)
local espOutlineColor = Color3.fromRGB(43, 179, 123)  -- ขอบขาวคงที่ หรือจะปรับได้ด้วย

-- ===== ฟังก์ชันช่วย =====
local function getPlayerList()
    local list = {}
    for _, p in ipairs(Players:GetPlayers()) do
        if p ~= player then
            table.insert(list, string.format("%s (%s)", p.DisplayName, p.Name))
        end
    end
    table.sort(list)
    return list
end

local function getPlayerFromSelection(selected)
    if not selected or selected == "" then return nil end
    local friendName = selected:match("%((.-)%)") or selected
    return Players:FindFirstChild(friendName)
end

-- ฟังก์ชันเปิด ESP (ใช้สีปัจจุบัน)
local function enableESP(targetPlayer)
    if not targetPlayer or not targetPlayer.Character then return end

    -- ลบ Highlight เก่าก่อน (กรณี respawn)
    if currentESPHighlight and currentESPHighlight.Parent then
        currentESPHighlight:Destroy()
    end

    local highlight = Instance.new("Highlight")
    highlight.Parent = targetPlayer.Character
    highlight.FillColor = espFillColor
    highlight.FillTransparency = 0.5
    highlight.OutlineColor = espOutlineColor
    highlight.OutlineTransparency = 0
    highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
    highlight.Enabled = true

    currentESPHighlight = highlight
    espTargetPlayer = targetPlayer
end

-- ฟังก์ชันปิด ESP
local function disableESP()
    if currentESPHighlight then
        currentESPHighlight:Destroy()
        currentESPHighlight = nil
    end
    if espConnection then
        espConnection:Disconnect()
        espConnection = nil
    end
    espTargetPlayer = nil
end

-- ฟังก์ชันอัปเดตสี ESP (เมื่อเปลี่ยนสีใน Color Picker)
local function updateESPColor()
    if currentESPHighlight and currentESPHighlight.Parent then
        currentESPHighlight.FillColor = espFillColor
        currentESPHighlight.OutlineColor = espOutlineColor
    end
end

local Label = friendTab:CreateLabel("Click Dropdown Player And Choose To Click", "cctv", Color3.fromRGB(43, 179, 123), false)

local Section1 = friendTab:CreateSection("")

-- ===== Dropdown เลือกผู้เล่น =====
local friendDropdown = friendTab:CreateDropdown({
    Name = "[ 👥 ]  Dropdown Player",
    Options = getPlayerList(),
    CurrentOption = {""},
    Flag = "FriendSelect",
    Callback = function()
        if currentESPHighlight then
            local selected = friendDropdown.CurrentOption[1]
            local newTarget = getPlayerFromSelection(selected)
            disableESP()
            if newTarget then
                enableESP(newTarget)
            end
        end
    end
})

-- อัปเดต Dropdown เมื่อมีคนเข้า-ออก
Players.PlayerAdded:Connect(function() 
    friendDropdown:Refresh(getPlayerList(), true)
end)

Players.PlayerRemoving:Connect(function(p)
    task.wait(1)
    friendDropdown:Refresh(getPlayerList(), true)
    
    if espTargetPlayer and p == espTargetPlayer then
        disableESP()
    end
end)

-- ===== ปุ่มวาปธรรมดา =====
friendTab:CreateButton({
    Name = "[ 📌 ]  Teleport To Player",
    Callback = function()
        local selected = friendDropdown.CurrentOption[1]
        if not selected or selected == "" then return end

        local targetPlayer = getPlayerFromSelection(selected)
        local char = player.Character or player.CharacterAdded:Wait()
        local hrp = char:WaitForChild("HumanoidRootPart", 5)
        if not hrp or not targetPlayer or not targetPlayer.Character then return end

        local targetHRP = targetPlayer.Character:FindFirstChild("HumanoidRootPart")
        if targetHRP then
            savedCFrame = hrp.CFrame
            hrp.CFrame = targetHRP.CFrame + Vector3.new(0, 5, 0)
        end
    end
})

-- ===== Auto Teleport Toggle =====
friendTab:CreateToggle({
    Name = "[ 🪑 ]  Auto Teleport to Player",
    CurrentValue = false,
    Flag = "AutoTPToggle",
    Callback = function(Value)
        local selected = friendDropdown.CurrentOption[1]
        if not selected or selected == "" then
            Rayfield.Flags["AutoTPToggle"]:Set(false)
            return
        end

        local friendName = selected:match("%((.-)%)") or selected
        local targetPlayer = Players:FindFirstChild(friendName)

        if Value then
            if not targetPlayer or not targetPlayer.Character then
                Rayfield.Flags["AutoTPToggle"]:Set(false)
                return
            end

            local char = player.Character or player.CharacterAdded:Wait()
            local hrp = char:FindForChild("HumanoidRootPart")
            if not hrp then
                Rayfield.Flags["AutoTPToggle"]:Set(false)
                return
            end

            if not savedCFrame then
                savedCFrame = hrp.CFrame
            end

            autoTargetName = friendName

            autoTPConnection = RunService.Heartbeat:Connect(function()
                if not autoTargetName then return end
                local target = Players:FindFirstChild(autoTargetName)
                if target and target.Character then
                    local targetHRP = target.Character:FindFirstChild("HumanoidRootPart")
                    if targetHRP and hrp then
                        hrp.CFrame = targetHRP.CFrame + Vector3.new(0, 4, 0)
                    end
                end
            end)
        else
            if autoTPConnection then
                autoTPConnection:Disconnect()
                autoTPConnection = nil
                autoTargetName = nil
            end
        end
    end
})

local Paragraph = friendTab:CreateParagraph({Title = "Teleport To Player / Friend Normal", Content = "Teleport To Player / Friend Become Normal"})

local Section1 = friendTab:CreateSection("")

-- ===== Color Picker สำหรับปรับสี ESP =====
friendTab:CreateColorPicker({
    Name = "[ 🎨 ]  Choose Color ESP Player",
    Color = Color3.fromRGB(43, 179, 123),
    Flag = "ESPColorPicker",
    Callback = function(Value)
        espFillColor = Value  -- อัปเดตสี fill
        -- espOutlineColor = Value  -- ถ้าอยากให้ขอบเปลี่ยนตามด้วย ให้ uncomment
        updateESPColor()  -- เปลี่ยนสี ESP ทันทีถ้ากำลังเปิดอยู่
    end
})

-- ===== ปุ่ม ESP (กดเพื่อสลับเปิด/ปิด) =====
friendTab:CreateButton({
    Name = "[ 👁️ ]  ESP Dropdown Player ( 43, 179, 123 )",
    Callback = function()
        local selected = friendDropdown.CurrentOption[1]
        local targetPlayer = getPlayerFromSelection(selected)

        if not targetPlayer then
            disableESP()
            return
        end

        if currentESPHighlight and espTargetPlayer == targetPlayer then
            disableESP()
        else
            disableESP()  -- ล้างเก่า
            enableESP(targetPlayer)

            -- ติดตาม respawn ของเป้าหมาย
            if espConnection then espConnection:Disconnect() end
            espConnection = targetPlayer.CharacterAdded:Connect(function()
                if espTargetPlayer == targetPlayer then
                    enableESP(targetPlayer)
                end
            end)
        end
    end
})

-- ===== ปุ่มกลับจุดเดิม =====
friendTab:CreateButton({
    Name = "[ 📷 ]  Return To The Point",
    Callback = function()
        local char = player.Character or player.CharacterAdded:Wait()
        local hrp = char:FindFirstChild("HumanoidRootPart")
        if hrp and savedCFrame then
            hrp.CFrame = savedCFrame
        end
    end
})

local Paragraph = friendTab:CreateParagraph({Title = "Teleport To Player / Friend Auto", Content = "Teleport To Player / Friend Become Auto"})

-- ปิดทุกอย่างเมื่อตัวเราตาย/respawn
player.CharacterRemoving:Connect(function()
    if autoTPConnection then
        autoTPConnection:Disconnect()
        autoTPConnection = nil
        autoTargetName = nil
        Rayfield.Flags["AutoTPToggle"]:Set(false)
    end
    disableESP()
end)

local Section1 = friendTab:CreateSection("")

--// === ESP TAB (Local ESP - Rainbow Edition) ===
local espTab = Window:CreateTab("||   - Attacks Smash", "drill")
local espSection = espTab:CreateSection("")

espTab:CreateButton({
    Name = "[ 🍭 ]  Teleport Tool  [ - Sword - ]",
    Callback = function()
        loadstring(game:HttpGet("https://pastebin.com/raw/DwGjVLUE"))()
    end
})

local Paragraph = espTab:CreateParagraph({Title = "Teleport Tool For Player / Friend", Content = "Teleport Tool Is Good For Building"})

local Section2 = espTab:CreateSection("")

local RainbowGun = {
    Enabled = false,
    Current = nil,
    Highlight = nil,
    Hue = 0,
    Connections = {},
    Loop = nil
}

local function CreateHighlight(tool)
    if RainbowGun.Highlight then RainbowGun.Highlight:Destroy() end
    RainbowGun.Highlight = Instance.new("Highlight")
    RainbowGun.Highlight.FillTransparency = 0.3
    RainbowGun.Highlight.OutlineTransparency = 0
    RainbowGun.Highlight.Parent = tool
    RainbowGun.Highlight.Adornee = tool:FindFirstChild("Handle") or tool
end

local function UpdateRainbowGun()
    if not RainbowGun.Enabled or not RainbowGun.Current or not RainbowGun.Highlight then return end
    RainbowGun.Hue = (RainbowGun.Hue + 0.01) % 1
    local color = Color3.fromHSV(RainbowGun.Hue, 1, 1)
    RainbowGun.Highlight.FillColor = color
    RainbowGun.Highlight.OutlineColor = color
    for _, part in pairs(RainbowGun.Current:GetDescendants()) do
        if part:IsA("BasePart") and part.Name ~= "HumanoidRootPart" then
            part.Color = color
        end
    end
end

local function OnEquipped(tool)
    if not RainbowGun.Enabled then return end
    RainbowGun.Current = tool
    task.wait(0.1)
    CreateHighlight(tool)
end

local function OnUnequipped()
    if RainbowGun.Highlight then RainbowGun.Highlight:Destroy() RainbowGun.Highlight = nil end
    RainbowGun.Current = nil
end

local function SetupGun(char)
    for _, c in pairs(RainbowGun.Connections) do c:Disconnect() end
    RainbowGun.Connections = {}

    local function ConnectTool(tool)
        table.insert(RainbowGun.Connections, tool.Equipped:Connect(function() OnEquipped(tool) end))
        table.insert(RainbowGun.Connections, tool.Unequipped:Connect(OnUnequipped))
    end

    for _, child in pairs(char:GetChildren()) do
        if child:IsA("Tool") then ConnectTool(child) end
    end
    table.insert(RainbowGun.Connections, char.ChildAdded:Connect(function(c)
        if c:IsA("Tool") then ConnectTool(c) end
    end))

    local backpack = LocalPlayer:WaitForChild("Backpack")
    for _, child in pairs(backpack:GetChildren()) do
        if child:IsA("Tool") then ConnectTool(child) end
    end
    table.insert(RainbowGun.Connections, backpack.ChildAdded:Connect(function(c)
        if c:IsA("Tool") then ConnectTool(c) end
    end))
end

local function ToggleRainbowGun(state)
    RainbowGun.Enabled = state
    if state then
        Rayfield:Notify({
           Title = "[ ✅ ] Rainbow Tools ON",
           Content = "[ 🌈 ] Rainbow Tools Finish",
           Duration = 3,
           Image = "rainbow",
         })
        if not RainbowGun.Loop then
            RainbowGun.Loop = RunService.Heartbeat:Connect(UpdateRainbowGun)
        end
        if LocalPlayer.Character then SetupGun(LocalPlayer.Character) end
    else
        Rayfield:Notify({
           Title = "[ ❌ ] Rainbow Tools OFF",
           Content = "[ 🌈 ] Rainbow Tools Finish",
           Duration = 3,
           Image = "rainbow",
         })
        if RainbowGun.Loop then RainbowGun.Loop:Disconnect() RainbowGun.Loop = nil end
        if RainbowGun.Highlight then RainbowGun.Highlight:Destroy() RainbowGun.Highlight = nil end
        RainbowGun.Current = nil
    end
end

LocalPlayer.CharacterAdded:Connect(SetupGun)
if LocalPlayer.Character then SetupGun(LocalPlayer.Character) end

espTab:CreateToggle({
    Name = "[ 🌈 ]  Rainbow Tool  [ - Sword - ]",
    CurrentValue = false,
    Callback = function(v) ToggleRainbowGun(v) end
})

Players.PlayerAdded:Connect(function(p)
    if ESPEnabled and p ~= LocalPlayer then
        p.CharacterAdded:Connect(function() createESP(p) end)
    end
end)

Players.PlayerRemoving:Connect(function(p)
    if ESPObjects[p] then
        for _, obj in pairs(ESPObjects[p]) do
            if typeof(obj) == "Instance" and obj.Remove then
                obj:Remove()
            end
        end
        ESPObjects[p] = nil
    end
end)

local Paragraph = espTab:CreateParagraph({Title = "Rainbow Tool For Cool Style", Content = "Rainbow Tool Is Good For Style"})

RunService.RenderStepped:Connect(updateESP)

local espSection = espTab:CreateSection("")

-- Variables
local ChamsEnabled = false
local HitboxEnabled = false
local OutlineColor = Color3.fromRGB(43, 179, 123)
local OutlineTransparency = 0
local FillTransparency = 0.7
local HitboxSize = 10
local HitboxTransparency = 0.3

local Highlights = {}

-- Chams Functions
local function AddHighlight(player)
    if player == game.Players.LocalPlayer then return end
    if Highlights[player] then Highlights[player]:Destroy() end
    
    local character = player.Character
    if not character then return end
    
    local highlight = Instance.new("Highlight")
    highlight.Parent = character
    highlight.Adornee = character
    highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
    highlight.Enabled = true
    highlight.FillColor = OutlineColor
    highlight.FillTransparency = FillTransparency
    highlight.OutlineColor = OutlineColor
    highlight.OutlineTransparency = OutlineTransparency
    
    Highlights[player] = highlight
end

local function RemoveAllHighlights()
    for _, highlight in pairs(Highlights) do
        if highlight then highlight:Destroy() end
    end
    Highlights = {}
end

-- Hitbox Functions
local function ApplyHitbox(player)
    if player == game.Players.LocalPlayer then return end
    local char = player.Character
    if not char then return end
    
    local head = char:FindFirstChild("Head")
    if head then
        head.Size = Vector3.new(HitboxSize, HitboxSize, HitboxSize)
        head.Material = Enum.Material.Neon
        head.Color = OutlineColor
        head.Transparency = HitboxTransparency
        head.CanCollide = false
    end
    
    local hrp = char:FindFirstChild("HumanoidRootPart")
    if hrp then
        hrp.Size = Vector3.new(HitboxSize/2, HitboxSize/2, 2)
        hrp.Material = Enum.Material.Neon
        hrp.Color = OutlineColor
        hrp.Transparency = HitboxTransparency
        hrp.CanCollide = false
    end
end

local function ResetHitbox(player)
    if player == game.Players.LocalPlayer then return end
    local char = player.Character
    if not char then return end
    
    local head = char:FindFirstChild("Head")
    if head then
        head.Size = Vector3.new(2, 1, 1)
        head.Material = Enum.Material.SmoothPlastic
        head.Color = Color3.fromRGB(163, 162, 165)
        head.Transparency = 0
        head.CanCollide = true
    end
    
    local hrp = char:FindFirstChild("HumanoidRootPart")
    if hrp then
        hrp.Size = Vector3.new(2, 2, 1)
        hrp.Material = Enum.Material.SmoothPlastic
        hrp.Transparency = 1
        hrp.CanCollide = false
    end
end

espTab:CreateToggle({
    Name = "[ ⚔️ ]  Hitbox Player",
    CurrentValue = false,
    Flag = "HitboxToggle",
    Callback = function(Value)
        HitboxEnabled = Value
        if Value then
            spawn(function()
                while HitboxEnabled do
                    for _, player in pairs(game.Players:GetPlayers()) do
                        if player ~= game.Players.LocalPlayer and player.Character then
                            ApplyHitbox(player)
                        end
                    end
                    task.wait()
                end
            end)
            game.Players.PlayerAdded:Connect(function(plr)
                plr.CharacterAdded:Connect(function() ApplyHitbox(plr) end)
            end)
        else
            for _, player in pairs(game.Players:GetPlayers()) do
                ResetHitbox(player)
            end
        end
    end,
})

espTab:CreateSlider({
    Name = "[ 📏 ]  Slider Hitbox Size",
    Range = {10, 2000},
    Increment = 1,
    Suffix = "Hitbox Size",
    CurrentValue = 10,
    Flag = "HitboxSize",
    Callback = function(Value)
        HitboxSize = Value
        if HitboxEnabled then
            for _, player in pairs(game.Players:GetPlayers()) do
                ApplyHitbox(player)
            end
        end
    end,
})

local Paragraph = espTab:CreateParagraph({Title = "Hitbox Player / Friend Sword", Content = "Hitbox Player / Friend Sword Are Muti Damage"})

local espSection = espTab:CreateSection("")

local NTab = Window:CreateTab("||   - Trolling Player", "smile")
local Section = NTab:CreateSection("")

local Players = game:GetService("Players")
local player = Players.LocalPlayer

-- ตัวแปรเก็บตำแหน่งเดิม
local savedCFrame = nil

-- ===== ฟังก์ชันช่วย =====
local function getPlayerList()
    local list = {}
    for _, p in ipairs(Players:GetPlayers()) do
        if p ~= player then
            table.insert(list, string.format("%s (%s)", p.DisplayName, p.Name))
        end
    end
    table.sort(list)
    return list
end

-- ===== Dropdown เลือกเพื่อน =====
local friendDropdown = NTab:CreateDropdown({
    Name = "[ 👥 ]  Dropdown Player  [ - 1 Sec - ]",
    Options = getPlayerList(),
    CurrentOption = {""},
    Flag = "FriendSelect",
    Callback = function() end
})

-- อัปเดต Dropdown เมื่อมีคนเข้า-ออก
Players.PlayerAdded:Connect(function()
    friendDropdown:Refresh(getPlayerList(), true)
end)
Players.PlayerRemoving:Connect(function()
    friendDropdown:Refresh(getPlayerList(), true)
end)

-- ===== ปุ่มวาปไปหาเพื่อน + กลับทันที =====
NTab:CreateButton({
    Name = "[ 📌 ]  Teleport To Player  [ - 1 Sec - ]",
    Callback = function()
        local selected = friendDropdown.CurrentOption[1]
        if not selected or selected == "" then
            return
        end

        local friendName = selected:match("%((.-)%)") or selected
        local targetPlayer = Players:FindFirstChild(friendName)
        local char = player.Character or player.CharacterAdded:Wait()
        local hrp = char:WaitForChild("HumanoidRootPart", 5)

        if not hrp then
            return
        end

        if targetPlayer and targetPlayer.Character then
            local targetHRP = targetPlayer.Character:FindFirstChild("HumanoidRootPart")
            if targetHRP then
                -- บันทึกตำแหน่งเดิม
                savedCFrame = hrp.CFrame

                -- วาปไปหาเพื่อน (ยกขึ้นนิดหน่อย)
                hrp.CFrame = targetHRP.CFrame + Vector3.new(0, 5, 0)

                -- แจ้งเตือนว่าวาปไปแล้ว

                -- รอเฟรมนิดนึงเพื่อให้เห็นการวาปชัดเจน (ไม่กระตุก)
                task.wait()

                -- กลับจุดเดิมทันที
                if savedCFrame and hrp and hrp.Parent then
                    hrp.CFrame = savedCFrame
                end

                return
            end
        end

    end
})

local Paragraph = NTab:CreateParagraph({Title = "Teleport Player / Friend And CheckPoint", Content = "Teleport Player / Friend And Mark Checkpoint"})

local Section = NTab:CreateSection("")

local Button = NTab:CreateButton({
   Name = "[ 📺 ]  Teleport Player All",
   Callback = function()
       local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local Character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
local HumanoidRootPart = Character:WaitForChild("HumanoidRootPart")
local originalCFrame = HumanoidRootPart.CFrame -- บันทึกตำแหน่งเดิม

-- สร้าง Highlight (วงสีเขียวเข้ม)
local function createHighlight(targetCharacter)
    local highlight = Instance.new("Highlight")
    highlight.FillColor = Color3.fromRGB(0, 120, 0) -- เขียวเข้ม (Fill)
    highlight.OutlineColor = Color3.fromRGB(0, 180, 0) -- เขียวเข้มกว่าอีก (Outline)
    highlight.FillTransparency = 0.4
    highlight.OutlineTransparency = 0
    highlight.Parent = targetCharacter
    return highlight
end

local function removeHighlight(targetCharacter)
    if targetCharacter:FindFirstChild("Highlight") then
        targetCharacter.Highlight:Destroy()
    end
end

-- สร้างแจ้งเตือนชื่อผู้เล่น (ScreenGui)
local function createNotification(playerName)
    local screenGui = Instance.new("ScreenGui")
    screenGui.Name = "TeleportNotification"
    screenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")
    
    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(0, 300, 0, 80)
    frame.Position = UDim2.new(0.5, -150, 0, 50)
    frame.BackgroundColor3 = Color3.fromRGB(0, 120, 0)
    frame.BorderSizePixel = 0
    frame.Parent = screenGui
    
    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 12)
    corner.Parent = frame
    
    local title = Instance.new("TextLabel")
    title.Size = UDim2.new(1, 0, 0.5, 0)
    title.Position = UDim2.new(0, 0, 0, 0)
    title.BackgroundTransparency = 1
    title.Text = "🌋 Teleport To"
    title.TextColor3 = Color3.fromRGB(255, 255, 255)
    title.TextScaled = true
    title.Font = Enum.Font.GothamBold
    title.Parent = frame
    
    local nameLabel = Instance.new("TextLabel")
    nameLabel.Size = UDim2.new(1, 0, 0.5, 0)
    nameLabel.Position = UDim2.new(0, 0, 0.5, 0)
    nameLabel.BackgroundTransparency = 1
    nameLabel.Text = playerName
    nameLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    nameLabel.TextScaled = true
    nameLabel.Font = Enum.Font.Gotham
    nameLabel.Parent = frame
    
    -- แอนิเมชันเข้า
    frame:TweenPosition(UDim2.new(0.5, -150, 0.1, 0), "Out", "Quad", 0.3, true)
    
    wait(0)
    
    -- แอนิเมชันออก
    frame:TweenPosition(UDim2.new(0.5, -150, -0.2, 0), "In", "Quad", 0.3, true)
    wait(1)
    screenGui:Destroy()
end

local function TeleportToPlayer(targetPlayer)
    if targetPlayer and targetPlayer.Character and targetPlayer.Character:FindFirstChild("HumanoidRootPart") then
        -- แจ้งเตือนชื่อผู้เล่น
        spawn(function() createNotification(targetPlayer.Name) end)
        
        -- สร้างวงสีเขียวเข้มรอบเป้าหมาย
        local highlight = createHighlight(targetPlayer.Character)
        
        -- วาปไปหาเป้าหมาย
        HumanoidRootPart.CFrame = targetPlayer.Character.HumanoidRootPart.CFrame * CFrame.new(0, 0, -3)
        wait(0) -- รอสักครู่เพื่อเห็นเอฟเฟค
        
        -- กลับตำแหน่งเดิม
        HumanoidRootPart.CFrame = originalCFrame
        
        -- ลบวงสีเขียวเข้ม
        removeHighlight(targetPlayer.Character)
    end
end

-- วนลูปวาปหาทุกคน
for _, player in pairs(Players:GetPlayers()) do
    if player ~= LocalPlayer then
        TeleportToPlayer(player)
        wait(0) -- รอก่อนไปคนถัดไป (เพิ่มเวลาเพราะมี notification)
    end
end   
   end,
})

local Paragraph = NTab:CreateParagraph({Title = "-- Teleport To Player / Friend All", Content = "-- Teleport To Player / Friend With 1 Second All"})

local Section = NTab:CreateSection("")

local Button = NTab:CreateButton({
   Name = "[ 💫 ]  Fling Player All",
   Callback = function()
       loadstring(game:HttpGet("https://pastebin.com/raw/zqyDSUWX"))()
	   Rayfield:Notify({Title = "💫  [ Fling Player All ]  💫", Content = "                     Finish 🏁", Duration = 2, lmage = "toggle-right"})
   end,
})

local Paragraph = NTab:CreateParagraph({Title = "-- Fling To Player / Friend All", Content = "-- Fling To Player / Friend With 5 Second All"})

local Section = NTab:CreateSection("")

local GTab = Window:CreateTab("||   - Script General", "globe")
local Section = GTab:CreateSection("")

local Button = GTab:CreateButton({Name = "|| -  Script-Admin", Callback = function() loadstring(game:HttpGet("https://raw.githubusercontent.com/BregUDidp/Banana-Hub/refs/heads/main/Admin"))() end})

local Section = GTab:CreateSection("")

GTab:CreateParagraph({
    Title = "Script Akari Hub Have So Many",
    Content = "The Script Have a Good Scipt  📟"
})

local Section = GTab:CreateSection("")

local Section = GTab:CreateSection("|| -  Script --Visual--  ||")

local Section = GTab:CreateSection("")

local Button = GTab:CreateButton({
   Name = "|| -  Script Infinite Yield",
   Callback = function()
       Rayfield:Notify({Title = "[ - Infinite Yield - ] -loading- --Finish--", Content = "-- Finish --", Duration = 2, Image = "bolt"})
       loadstring(game:HttpGet('https://raw.githubusercontent.com/DarkNetworks/Infinite-Yield/main/latest.lua'))()
   end,
})

local Button = GTab:CreateButton({
   Name = "|| -  Script Hitboxs",
   Callback = function()
       Rayfield:Notify({Title = "[ - Hitboxs - ] -loading- --Finish--", Content = "-- Finish --", Duration = 2, Image = "bolt"})
       loadstring(game:HttpGet("https://pastefy.app/ItfO0tdg/raw"))()
   end,
})

local CTab = Window:CreateTab("||   - Rejoin Sever", "git-pull-request-closed")
local Section = CTab:CreateSection("")

local Button = CTab:CreateButton({
   Name = "Rejoin Sever  [ - One Click - ]",
   Callback = function()
       local TeleportService = game:GetService("TeleportService")
          local Players = game:GetService("Players")
              local LocalPlayer = Players.LocalPlayer
              TeleportService:Teleport(game.PlaceId, LocalPlayer)

               game.StarterGui:SetCore("SendNotification", {
                   Title = "⏰ Loading Wait Rejoin Map",
                   Text = "________________________________________________________",
               Duration = 5
            })
   end,
})

local Section = CTab:CreateSection("")

CTab:CreateParagraph({
    Title = "Rejoin Sever One Click",
    Content = "The Rejoin Sever Have a Mini Scipt  🌀"
})
